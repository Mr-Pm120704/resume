# Bright Data Clone - Full MERN (Vite + Tailwind)

This repo contains frontend and backend. See instructions in README.

## Docker (optional)

Run:
```
docker-compose up --build
```
This will start mongo, backend (5000) and frontend (3000 -> nginx serving React build).
